<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">

</head>
<body>
    <a href="https://github.com/m-allanson/pelican-themes"><img style="position: absolute; top: 0; right: 0; border: 0;" src="https://s3.amazonaws.com/github/ribbons/forkme_right_white_ffffff.png" alt="Fork me on GitHub"></a>
    <div class="container">
        <div class="row-fluid">
            <div class="span8">
                <h1><%= title %></h1>
                <p>A list of <strong><%= themes.length %></strong> themes available from the <a href="https://github.com/getpelican/pelican-themes">Pelican Github repository</a></p>
            </div>
        </div>
        <div class="row-fluid">
            <div id="themes" class="span12">

                <% for (var theme in themes) {
                        var screenshots = themes[theme].images
                %>

                        <div class="theme">
                            <h2><a href="https://github.com/<%= themes[theme].user %>/<%= themes[theme].repo %>/tree/<%= themes[theme].sha %>/<%= themes[theme].path %>" target="_blank"><%= themes[theme].name %></a></h2>

                            <% if (screenshots.length > 0) { %>
                                <!-- TODO: Some themes have multiple screenshots which we could show -->
                                <img src="<%= screenshots[0] %>" alt="<%= themes[theme].path %>">

                            <% } else { %>
                                <p>There aren't any screenshots for this theme :(</p>
                            <% } %>
                        </div>
                <% } %>

            </div>
        </div>
    </div>


    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
      ga('create', 'UA-2364042-8', 'pelicanthemes.com');
      ga('send', 'pageview');
    </script>
</body>
</html>
